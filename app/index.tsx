import AdaptiveUI from "@/compoments/AdaptiveUI";
import { SafeAreaView } from "react-native";

export default function Index() {
  return (
   <SafeAreaView style={{ flex: 1 }}>
    <AdaptiveUI />
   </SafeAreaView>
  );
}
